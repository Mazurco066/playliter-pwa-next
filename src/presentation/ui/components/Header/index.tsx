// Dependencies
import { FC } from 'react'

// Components
import { Container } from '@chakra-ui/react'

// Header component
export const Header: FC = () => {
  // JSX
  return (
    <Container>
      <p>Header</p>
    </Container> 
  )
}