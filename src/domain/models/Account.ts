export type AccountType = {
  avatar: string,
  createdAt: string,
  updatedAt: string,
  email: string,
  id: string,
  isEmailconfirmed: boolean,
  name: string,
  role: string,
  username: string
}