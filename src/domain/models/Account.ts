export type AccountType = {
  id: string,
  userId: string,
  createdAt: string,
  updatedAt: string,
  avatar: string,
  email: string,
  isEmailconfirmed: boolean,
  name: string,
  role: string,
  username: string
}